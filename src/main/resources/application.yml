spring:
  application:
    name: AuthService
  profiles:
    active: dev
  jackson:
    date-format: com.fasterxml.jackson.databind.util.ISO8601DateFormat
    time-zone: UTC

server:
  port: ${port:8080}